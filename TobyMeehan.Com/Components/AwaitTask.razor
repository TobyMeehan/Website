@if (Model == null)
{
    <Loading></Loading>
}
else
{
    @ChildContent
}

@code {
    [Parameter] public object Model { get; set; }

    [Parameter] public RenderFragment ChildContent { get; set; }
}
