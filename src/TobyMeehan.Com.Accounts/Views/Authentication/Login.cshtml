@model TobyMeehan.Com.Accounts.Models.Authentication.Login.LoginViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<h2 class="text-3xl font-extralight mb-7 text-center">Log In</h2>

<form method="post">
    
    <div class="my-3">
        <label class="block text-md mb-1.5"
               asp-for="Form.Username">
            Username
        </label>
        <input class="form-control"
               type="text" asp-for="Form.Username"/>
        <span class="text-sm text-negative"
              asp-validation-for="Form.Username"></span>
    </div>

    <div class="my-3">
        <label class="block text-md mb-1.5"
               asp-for="Form.Password">
            Password
        </label>
        <input class="form-control"
               type="password" asp-for="Form.Password"/>
        <span class="text-sm text-negative" asp-validation-for="Form.Password"></span>
    </div>

    <button type="submit" class="btn btn-primary my-3 w-full">Log In</button>

    <p>Don't have an account? <a class="link" asp-controller="Authentication" asp-action="Register" asp-route-ReturnUrl="@Model.ReturnUrl">Register</a></p>
</form>

@section Scripts
{
    <partial name="_ValidationScriptsPartial"/>
}